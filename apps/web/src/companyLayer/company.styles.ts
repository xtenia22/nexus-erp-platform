import { company } from "./company.config";

export const companyStyles = {
  primaryColor: company.theme.colors.primary,

  primaryButton: {
    backgroundColor: company.theme.colors.primary,
    color: "#FFFFFF",
  },

  primaryBorder: {
    borderColor: company.theme.colors.primary,
  },

  primaryText: {
    color: company.theme.colors.primary,
  },
};